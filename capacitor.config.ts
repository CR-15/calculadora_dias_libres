import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.esteban.calculadoradiaslibres',
  appName: 'Días Libres',
  webDir: 'out',
  android: {
    backgroundColor: '#f3f6f9',
  },
};

export default config;
